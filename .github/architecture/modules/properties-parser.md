# Properties Parser Module

Properties files are parsed with `java.util.Properties` through a UTF-8 reader. Parse failures are wrapped in `ConfigParseException` with source metadata.

This keeps v0.1.0 compatible with standard `.properties` syntax.
