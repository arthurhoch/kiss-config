# Error Model Module

All project exceptions extend `ConfigException`.

Exceptions should include the key, target type where relevant, source name or path when available, safe masked values, and actionable guidance.
