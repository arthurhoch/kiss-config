# Type Conversion Module

Supported target types include strings, primitive and wrapper numbers, booleans, `BigDecimal`, `BigInteger`, `Duration`, Java time types, enums, and comma-separated lists of supported scalar types.

Duration supports ISO-8601 and simple forms such as `30s`, `5m`, `2h`, `1d`, and `500ms`.
