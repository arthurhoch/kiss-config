# Dependency Policy

KissConfig has zero production dependencies. The Java standard library is the only production runtime dependency.

JUnit Jupiter is allowed for tests. New production dependencies require explicit human approval and a documented architecture decision.
