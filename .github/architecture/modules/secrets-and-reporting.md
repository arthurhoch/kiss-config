# Secrets And Reporting Module

Values are masked when a mapped component is annotated with `@Secret` or when the canonical key looks secret, such as password, passwd, secret, token, api.key, private.key, or credential.

Reports include attempted sources, loaded sources, skipped sources, effective values, merge strategy, profile, configured file names, and search order.
