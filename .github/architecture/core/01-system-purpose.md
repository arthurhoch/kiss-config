# System Purpose

KissConfig loads `.properties` files, explicit `.env` files, Java system properties, and operating-system environment variables into immutable typed Java objects, especially records.

The public value proposition is:

> Load configuration from properties, .env files, system properties, and environment variables directly into immutable Java records.
