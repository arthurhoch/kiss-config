# AI Behavior Rules

AI agents must read `README.md`, `.github/ALL_MARKDOWN.md`, and this architecture index before non-trivial work.

Agents must not add dependencies, weaken tests, expose secret values, or introduce framework-specific code. Behavior changes require docs, tests, and changelog updates.
