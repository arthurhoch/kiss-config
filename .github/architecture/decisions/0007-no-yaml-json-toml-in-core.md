# 0007 No YAML JSON TOML In Core

## Decision

KissConfig v0.1.0 supports `.properties`, explicit `.env`, Java system properties, and OS environment variables. YAML, JSON, and TOML are out of scope.

## Consequence

The core remains small and zero-dependency.
